<?xml version="1.0" encoding="UTF-8"?>
<configuration>
    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} [%t] %-5level %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>

    <!-- Application : INFO en prod, passer à DEBUG pour investiguer -->
    <logger name="com.interview.boat" level="INFO" additivity="false">
        <appender-ref ref="CONSOLE"/>
    </logger>

    <!-- Réduire le bruit Spring / Hibernate -->
    <root level="WARN">
        <appender-ref ref="CONSOLE"/>
    </root>
</configuration>

